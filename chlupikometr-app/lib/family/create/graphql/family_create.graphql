mutation familyCreate($input: FamilyCreateInput!) {
    familyCreate(input: $input) {
        family {
            id
            name
        }
        errors {
            code
            message
        }
    }
}
